course_docs_name: "LFS258_V2025-06-18_SOLUTIONS"
course_docs_dir: "/opt/course-docs"

# Bitwarden Secrets Manager Config
# Access token read from the gitignored secrets.yml at the repo root
bws:
  organization_id: "d8036b70-0b7f-4c86-adca-ae970103ab42"
  secret_ids:
    lfs_cm_username: "63754df3-601f-4cfd-afac-b39300fa985e"
    lfs_cm_password: "d7da3a30-bfd0-4cb0-b2fa-b39300faad29"

k8s_ssh_user: "abramley"

bridge_nf_call_ip6tables: 1
bridge_nf_call_iptables: 1
ipv4_ip_forward: 1

keyrings_dir: /etc/apt/keyrings

kubernetes_version_apt: "1.32.1-1.1"

crictl_arch_map:
  x86_64: amd64
  aarch64: arm64
  armv7l: arm

crictl_arch: "{{ crictl_arch_map[ansible_architecture] }}"
crictl_version: "1.32.0"

# Kubeadm config values
kubernetes_version_kubeadm: "1.32.1"
podSubnet: "192.168.0.0/16"
